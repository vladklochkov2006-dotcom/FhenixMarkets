{
  "name": "fhenix-markets",
  "version": "1.0.0",
  "private": true,
  "description": "Privacy-preserving prediction markets with FHE on Ethereum Sepolia",
  "author": "Fhenix Markets Team",
  "license": "MIT",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint",
    "compile": "cd contracts-fhenix && npx hardhat compile",
    "deploy": "cd contracts-fhenix && npx hardhat run scripts/deploy.ts --network sepolia",
    "test:contracts": "cd contracts-fhenix && npx hardhat test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
